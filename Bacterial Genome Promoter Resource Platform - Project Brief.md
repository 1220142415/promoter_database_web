# 细菌基因组启动子资源网站

> 面向微生物基因组研究者的启动子预测结果浏览、检索和下载平台。

## 1. 项目要解决的问题

研究细菌启动子时，通常需要在基因组数据库、注释数据库和预测工具之间反复切换。单个基因组尚且如此，面对数万条基因组记录时，结果查找、位置核对和批量下载会更加耗时。

本项目把已经生成的启动子预测结果与基因组序列、基因注释放在同一个入口中。用户可以先找到目标基因组，再在浏览器中查看预测窗口与附近基因，最后下载原始记录或用于后续分析的序列。

首批数据计划覆盖约 80,000 个 GTDB 细菌基因组。正式发布时需要注明 GTDB release、数据版本、纳入规则和数据更新时间。

## 2. 用户与主要任务

| 用户 | 主要任务 |
| --- | --- |
| 实验研究者 | 定位目标基因或基因组区域，查看附近的预测启动子，核对方向和序列，导出候选位点用于实验验证。 |
| 生物信息学研究者 | 按 GTDB 分类和基因组属性筛选数据，比较多个基因组的预测结果，下载标准化文件进行下游分析。 |

## 3. 用户流程

### 单个基因组

搜索基因组或基因 → 打开基因组详情 → 跳转到目标 contig 和区间 → 查看基因、预测启动子和实验 TSS → 下载记录或序列。

### 多个基因组

选择 GTDB 分类或筛选条件 → 查看符合条件的基因组数量 → 检查数据规模和版本 → 选择文件格式 → 创建批量下载任务。

## 4. 第一阶段范围

第一阶段先保证“找得到、看得懂、下得来、能追溯”。

### 必须支持

- 按 GTDB/NCBI 基因组编号、物种名称和分类信息检索。
- 按 GTDB 分类层级、组装水平、基因组大小等属性筛选，并显示结果数量。
- 查看基因组元数据、预测窗口数量和可用注释。
- 使用基因组浏览器查看参考序列、基因注释、预测启动子和分数轨道。
- 支持坐标跳转、缩放、平移和点击查看记录。
- 下载单个预测窗口、单个基因组和筛选结果对应的数据。
- 提供数据来源、版本信息和推荐引用方式。

### 暂不作为第一阶段要求

- 基因名或 locus tag 的全库检索。
- 面向程序化访问的完整 API。
- 多基因组在线比较、自定义轨道和分享链接。

## 5. 基因组浏览器

浏览器采用 JBrowse 2。每个基因组需要绑定明确的 assembly 和参考序列，不能用一个固定 assembly 代替所有基因组。

建议至少提供以下轨道：

| 轨道 | 数据 | 说明 |
| --- | --- | --- |
| 参考序列 | FASTA | 显示 contig 和碱基序列。 |
| 基因与 CDS | GFF3 | 显示基因、CDS 及其注释。 |
| 预测启动子 | BED 或 GFF3 | 显示模型预测的区间、链方向和 score。 |
| 实验验证 TSS | BED 或 GFF3 | 单独显示有实验依据的转录起始位点，并保留来源信息。 |
| Terminator | BED 或 GFF3 | 显示终止子注释（如果该数据集提供）。 |

预测启动子和实验验证 TSS 是两类不同证据，必须在数据表、浏览器轨道、筛选条件和下载文件中分开表示。没有实验 TSS 的基因组应明确显示“暂无实验验证 TSS”，不能据此推断不存在 TSS。

选中区间后，可以导出该区间的参考序列、基因注释、预测启动子和实验 TSS 记录。

## 6. 检索与筛选

网站提供两种入口：

- **直接检索**：按基因组 ID、物种名称、分类名称或坐标定位。坐标应包含基因组、contig 和区间，例如 `genome_id:contig:start-end`。
- **条件筛选**：按 GTDB 分类、组装质量、GC 含量、基因组大小、预测启动子数量或 score 阈值筛选。

检索结果应支持排序、分页、导出，并能跳转到对应的基因组详情或浏览器位置。面向数万个基因组的模糊搜索和统计需要数据库索引或服务端聚合，不能只依赖浏览器加载全部数据。

## 7. 数据格式与下载

### 单个预测窗口

包括基因组 ID、contig、起止坐标、链方向、模型 score、窗口序列、模型版本和数据版本。附近基因字段是否提供，取决于注释规则。

### 单个基因组

建议提供：

- 基因组序列：FASTA；
- 基因注释：GFF3；
- 预测启动子：BED 和/或 GFF3；
- 实验验证 TSS：单独的 BED 和/或 GFF3；
- Terminator 注释（如有）；
- 预测窗口表、基因组元数据和 manifest。

基因组 FASTA 和完整注释能否重新分发，需要根据原始数据许可证确认。

### 批量数据

批量下载包应包含筛选条件 `query.json`、基因组清单、相应序列、manifest 和版本说明。数据量较大时采用服务端异步打包，并提供任务状态和校验信息。

后续可以增加启动子序列提取：用户设置上游和下游长度后，生成用于 motif 分析的 FASTA。该功能需要先明确启动子定义、正负链处理方式。

## 8. 数据与方法说明

网站应提供一个简洁的“数据与方法”页面，至少说明：

- 数据来源、GTDB release 和纳入范围；
- 基因组组装和注释版本；
- 预测模型版本、扫描窗口、步长和筛选阈值；
- 预测启动子与实验验证 TSS 的证据来源；
- 数据许可证、更新时间和推荐引用方式。


坐标体系也要在页面和下载说明中写清楚。数据库、BED 和 JBrowse 建议统一使用 0-based、half-open 坐标；GFF3 导入时再按规则转换，避免同一位点在不同文件中出现偏移。

## 9. 第一版验收标准

第一版完成后，用户应能：

1. 搜索并打开一个目标基因组；
2. 在同一视图中查看参考序列、基因注释和预测启动子；
3. 单独查看实验验证 TSS（若有）；
4. 下载单个基因组的 FASTA、GFF3、BED 和元数据；
5. 查到数据版本、模型说明、证据来源和引用信息。

批量下载、组合筛选、全基因组概览和多基因组比较可以在上述流程稳定后再加入。

## 10. 典型使用过程

~~~text
搜索目标基因组或基因
        ↓
在浏览器中查看基因、预测启动子和实验 TSS 的相对位置
        ↓
按 score 或坐标选择记录
        ↓
下载 BED、GFF3、FASTA 或提取的启动子序列
        ↓
在本地进行 motif 分析或比较基因组学分析
~~~

## 11. 需要提前确认的事项
- BED、GFF3、数据库和 JBrowse 之间的坐标转换规则。
- 实验 TSS 的来源字段、实验条件、文献或数据库链接。
- 80,000 个基因组的索引、分页、统计和批量打包方案。

## 12. 预期结果

用户可以从一个入口完成启动子预测结果的检索、定位、阅读、下载和引用。
