# 本地免登录真实预测验收记录 — 2026-09-07

## 授权后当前状态

管理员授权已完成，Wrangler 凭据已保存。测试票据接口及专用 Secret 已部署，本地 `http://127.0.0.1:3000/predict` 实测无需邮箱或 Turnstile，即可提交真实模型任务。

| 检查 | 当前证据 |
| --- | --- |
| 最小生产发布 | 基于 `85c2ae5f-8dc2-4b3f-8ba1-fcff5bd64ce5`，追加票据模块后激活 `2e9f5cfc-807d-4d42-bd68-52577c90f45e`；保留全部原有绑定和 Secrets，未更改 D1 schema、路由或模型服务 |
| 真实版本预览 | 缺失/错误授权 401、正确授权和 D1 就绪 200、输入超额 413、错误模型 400、错误 HTTP 方法 405；原 `/predict` 及脚本路径相同，抽查静态资产逐字节相同 |
| 本地提交 | 100 bp 和完整基因组均经本地接口自动取真实票据并被线上服务接收；浏览器显示本地模式，真实示例校验完成后按钮可用 |
| 线上票据消费 | 真实票据消费后重复提交 401；输入超过票据允许值 401；等待实际 TTL 过期后用限额内输入提交 401。额外短序列探针 `fba27e8d4daf4781a3444fcc8b8c8089` 用于重放验证，无重复基因组任务 |
| 100 bp 真实任务 | `f8ae668c58e449a7920d773471bc7ae0` 已成功推理；完整 4,641,652 bp 用作 CGR；模型和 checkpoint 身份相符 |
| 短序列验收失败 | 请求双链，服务仅返回正链 1 个窗口，分数 `0.0058243670500814915`。页面显示实际正链分数及缺失反链提示，验收器保持失败，不生成第二条分数 |
| 基因组任务 | `55f6a8a25ebb44a2a375c773f2e3cf7a`，完整 `NC_000913.3`，stride 1、双链；先进入 scanning，随后真实服务返回 failed。未通过 9,283,106 个窗口、轨道、产物及 Range 验收；本次未重提任务或更改模型服务 |
| 针对性回归 | 追加入口及原路由转发 24 项、验收器契约/只读网络恢复 8 项、真实参考与部分结果展示 18 项通过；相关 lint、TypeScript 通过 |
| 浏览器恢复 | 100 bp 结果刷新后仍显示同一真实任务及正链分数 0.005824；基因组页刷新后恢复同一 running/scanning 任务；结果页未观察到控制台警告/错误 |

短序列 `scores.json` 为 133 bytes，SHA-256 `ae9eacb44d4b3989f34b9a4ae1a0692ce07a3c679071c64e4e2c98357754bd0e`；`summary.json` 为 829 bytes，SHA-256 `9adec24922f6a9e876f0bbb7c383118cf2962cee6d0c71a6ae4abd6f4d860d2a`。当前模型仍为 `candidate-github-93cf` 候选模型，本次未改动或重启模型服务。

当前可查看的本地页面（同一浏览器已保存访问权限）：

- [100 bp 结果](http://127.0.0.1:3000/predict/task/f8ae668c58e449a7920d773471bc7ae0)
- [完整基因组任务](http://127.0.0.1:3000/predict/task/55f6a8a25ebb44a2a375c773f2e3cf7a)

持续更新的无凭据运行记录：`.codex-runtime/prediction-live/20260907-local-authorized/report.json`。预览及部署证据在 `.codex-runtime/cloudflare-release-20260907/`。访问令牌只存于忽略的 private 文件和浏览器任务会话；公开报告不包含令牌或测试密钥。验收器遇到短序列契约失败后继续独立基因组任务，最终总体结果仍保留失败；恢复同一运行目录不会重复提交已有任务。

本次未宣称本地整站 Next/OpenNext 生产构建通过；实际发布候选是原线上模块加独立编译的票据模块，已完成该候选的 Cloudflare 预览校验。整站源代码发布前仍需同步线上改动并完成完整构建。

## 授权前的历史检查

同日经现有生产公开入口成功提交过一个免邮箱登录的真实 100 bp 任务，见 [线上入口实测](prediction-public-use-2026-09-07.md)。该路径保留 Turnstile。以下保留最初未授权阶段的检查和阻塞记录，已由上面的当前状态更新。

本地实现已保留。用户选择暂不授权 Cloudflare 管理员登录，因此远端票据接口和专用 Secret **未发布**，本轮没有创建任何模型任务。真实分数、完整基因组轨道和产物验收仍未完成。

| 检查 | 本轮证据 |
| --- | --- |
| 本地免登录模式 | 实际浏览器显示“本地真实预测测试”，不要求邮箱登录或人机验证 |
| 网络边界 | 实际服务仅监听 `127.0.0.1:3000`；`localhost` 别名可用；跨站 Origin、缺少 Origin、非本地 Host 均返回正式认证错误 |
| 生产边界 | 回归验证 `NODE_ENV=production`、非本地部署、开关关闭等情形均不启用本地例外；旧版本 API 仍要求认证 |
| 专用密钥 | 已在忽略的 `.env.prediction-local` 生成；未上传；未复用模型服务密钥；票据响应仅返回允许的字段 |
| 远端内部接口 | 已实现受密钥保护的 GET 可用性检查和 POST 签发，复用原有 D1 表、限额和票据消费流程；缺失/错误密钥、模型及输入限制已回归 |
| 票据 SQL | 一次性内存 SQLite 执行真实 `0008`、`0011` 迁移和实际签发/消费 SQL，通过有效消费、重放、精确过期边界、错误模型、输入超限、分钟/每日基因组额度及哈希存储检查 |
| 用户及邮件 | 本地任务不调用用户认证、不预留用户每日扫描配额、不登记或发送邮件；正式提交逻辑保留 |
| 固定真实输入 | 验收器通过 `GCF_000005845.2` / `NC_000913.3`、4,641,652 bp 及全部参考校验；100 bp 正链片段按 100001–100100 重提取 |
| 浏览器输入 | 实际加载并校验短示例及完整基因组 CGR；核对双链、stride 1、禁用不支持的筛选；重新检查可用性保留输入；未观察到控制台警告/错误 |
| 自动取票验收器 | 实际运行经本地入口请求远端票据，收到 `TEST_TICKETS_DISABLED`（HTTP 404），记录 `blocked_credentials`；没有生成假票据或提交模型任务 |
| 完整检查 | 隔离目录执行 `npm ci`，lint、TypeScript、92 个文件 / 624 项测试通过；最终地址规范化和响应限制改动后，65 项定向测试、TypeScript 和 lint 再次通过 |
| 真实结果、窗口和轨道 | 未运行：没有真实任务；尚未验证 9,283,106 个窗口、真实分数、产物 SHA-256、轨道缩放/碱基显示、任务刷新恢复及真实 BigWig/FAI Range |

锁文件一致的验证环境：Next 15.5.24、React 19.2.8、OpenNext 1.20.4、Wrangler 4.127.0；检查使用 Node 24.19.0。当前可见开发服务保留原有安装（Next 15.5.21、React 19.2.4），使用 Node 24.19.0 和 Turbopack。隔离的 Windows Worker 构建尚未验收完成，不能将本地检查结果当作发布就绪证明。

构建分别尝试 Node 24 和经官方 SHA-256 核验的 Node 22.18.0；补齐隔离目录的 `.gitignore` 后仍停留在 `Creating an optimized production build ...`。已停止这些构建进程，保留日志；原因未确认，没有将该观察归为功能测试失败。发布构建、产物排查和 Worker 预览需要后续完成。

真实参考 FASTA SHA-256：`53bb6a51b6e92139ced1e38f74b7938781027c52200922ff03718c2237d23bb4`。在线 readiness/model 查询可用，模型保持 `candidate-github-93cf` 的候选身份；真实基因组来源不意味着片段已被实验证实为启动子。

不含凭据的运行记录位于忽略目录：

- `.codex-runtime/prediction-live/20260907-local-no-login/report.json`
- `.codex-runtime/prediction-live/ticket-sql-report.json`
- `.codex-runtime/prediction-live/local-boundary-report.json`
- `.codex-runtime/prediction-build-20260907/check-clean.log`

Cloudflare OAuth 最初被自动审批拦截，Wrangler 又自动追加 `offline_access`；已在浏览器授权前停止该流程。随后用户明确选择“暂不授权，保留本地改动”。未改变生产认证、远端 D1、Worker Secrets 或模型服务。后续若恢复授权，先完成发布构建/预览并核验现有远端配置，再按 [配置说明](prediction-local-test.md) 发布专用 Secret 和接口，最后重新运行独立真实验收器。
